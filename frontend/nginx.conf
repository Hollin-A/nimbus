server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Security headers for paths that don't define their own add_header
    # (the SPA fallback below). The same snippet is re-included inside
    # each cache-controlled location for the inheritance reason explained
    # in security-headers.conf.
    include /etc/nginx/snippets/security-headers.conf;

    # SPA fallback — every unknown path renders the app shell so the
    # client-side router can take over.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Hashed bundle assets — safe to cache aggressively.
    location /assets/ {
        include /etc/nginx/snippets/security-headers.conf;
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # PWA control plane — these MUST revalidate so a new deploy
    # actually replaces the running service worker on the next visit.
    location = /index.html {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
    }
    location = /sw.js {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
    }
    location = /registerSW.js {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
    }
    location = /manifest.webmanifest {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
    }

    # Gzip text-y responses on the fly.
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 256;
}
